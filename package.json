{
  "name": "aws-github-webhook-listener",
  "version": "1.0.0",
  "description": "Extend your github repositories with this github webhook listener",
  "main": "handler.js",
  "scripts": {
    "deploy": "env-cmd deploy-env serverless deploy -v",
    "logs": "env-cmd deploy-env serverless logs -f githubWebhookListener -t"
  },
  "author": "David Wells",
  "license": "MIT",
  "dependencies": {
    "trello": "^0.9.0"
  },
  "devDependencies": {
    "env-cmd": "^8.0.2"
  }
}
